# 🔗 Handoff: Scheduler Improvement

## Context Chain

| # | Workflow | Date | Kết quả | Ghi chú |
|---|----------|------|---------|---------|
| 1 | /create-plan (manual) | 2026-03-25 | ✅ Plan created | Plan scheduler startup catch-up + management APIs |
| 2 | /review-plan | 2026-03-25 | ⚠️ CẦN BỔ SUNG | 4 điểm cần bổ sung: blocking/non-blocking, SJC limitation, SQLite locking, scheduler enable/disable config |

## Files đã phân tích

| File | Vai trò |
|------|---------|
| `backend/app/scheduler.py` | Scheduler hiện tại — APScheduler BackgroundScheduler, 2 jobs (daily collect 08:00, weekly retrain Mon 09:00) |
| `backend/app/main.py` | Lifespan — gọi `start_scheduler()` / `stop_scheduler()`, health check |
| `backend/app/config.py` | Config — `daily_collect_time`, `weekly_retrain_day` |
| `backend/app/api/routes/gold_routes.py` | API routes — `POST /collect-data` |
| `backend/app/services/data_collector/data_pipeline.py` | DataPipeline — orchestrator, `run_all()`, `run_gold_only()`, etc. |
| `backend/app/services/data_collector/xau_collector.py` | XAU collector — incremental fetch yfinance, hỗ trợ backfill |
| `backend/app/services/data_collector/sjc_collector.py` | SJC collector — chỉ fetch giá hôm nay, KHÔNG backfill |
| `backend/app/services/data_collector/base_collector.py` | Base collector — `collect_and_store()` tự tạo session riêng |
| `backend/app/db/database.py` | DB — SQLite + `check_same_thread=False` |

## Decisions đã chốt

- Scheduler đã tồn tại, chỉ cần cải thiện (không tạo mới)
- Dùng APScheduler one-off job cho startup catch-up (đề xuất từ review)

## Next

- Cập nhật plan theo 4 điểm bổ sung → `/implement-plan .agent/plans/scheduler-improvement/implementation_plan.md`
